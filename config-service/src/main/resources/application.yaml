server:
  port: 8081
spring:
  cloud:     
   config:
      server:
        git:
          #default-label: config-server
          default-label: eureka
          uri: https://github.com/rueb/microservicio
          search-paths: config-data
  application:
    name: config-service
    
    
